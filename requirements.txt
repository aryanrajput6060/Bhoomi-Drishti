# BHUMI INSIGHT — backend runtime dependencies (Render / local)
# Pinned to the versions currently running locally.
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5

# ---------------------------------------------------------------------------
# JARVIS — required dependencies
# The JARVIS runtime imports these modules directly; keep them pinned.
# ---------------------------------------------------------------------------

# Core server + validation
python-dotenv>=1.0.1

# LLM client (OpenAI-compatible)
httpx>=0.27.0

# Speech recognition (optional but referenced in stt.py)
# faster-whisper>=0.10.0          # installs ctranslate2 + onnxruntime automatically
# numpy                            # pulled in by faster-whisper
# sounddevice                      # microphone capture (optional)

# Text-to-speech / media helpers used by jarvis skills
# edge-tts                         # Edge TTS voice (optional)
# Pillow                           # image / screenshot helpers (optional)

# Database
# sqlite3 is part of the standard library; no extra package required.

